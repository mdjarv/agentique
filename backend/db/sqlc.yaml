version: "2"
sql:
  - engine: "sqlite"
    queries: "queries/"
    schema: "migrations/"
    gen:
      go:
        package: "store"
        out: "../internal/store"
        emit_json_tags: true
        emit_empty_slices: true
        emit_interface: true
